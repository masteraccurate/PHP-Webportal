function smiley(a)
{
 if(a==1)
 window.document.ADD.comment.value=window.document.ADD.comment.value+":)";
 if(a==2)
 window.document.ADD.comment.value=window.document.ADD.comment.value+";)";
 if(a==3)
 window.document.ADD.comment.value=window.document.ADD.comment.value+":D";
}
function smileys(a)
{
 if(a==1)
 window.document.ADD.message.value=window.document.ADD.message.value+":)";
 if(a==2)
 window.document.ADD.message.value=window.document.ADD.message.value+";)";
 if(a==3)
 window.document.ADD.message.value=window.document.ADD.message.value+":D";
}
function symbol(a)
{
 if(a==1)
 window.document.ADD.comment.value=window.document.ADD.comment.value+"{br}";
}
function symbols(a)
{
 if(a==1)
 window.document.ADD.message.value=window.document.ADD.message.value+"{br}";
}